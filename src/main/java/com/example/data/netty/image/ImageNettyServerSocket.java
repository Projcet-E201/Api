package com.example.data.netty.image;

// @Component
// public class ImageNettyServerSocket extends NettyServerSocket {
// 	public ImageNettyServerSocket(ServerBootstrap serverBootstrap, @Qualifier("imageSocketAddress") InetSocketAddress tcpPort) {
// 		super(serverBootstrap, tcpPort);
// 	}
// }
